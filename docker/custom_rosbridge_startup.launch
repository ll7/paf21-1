<!-- -->
<launch>
  <!-- launch the CARLA ROS bridge -->
  <include file="$(find carla_ros_bridge)/launch/carla_ros_bridge.launch">
    <arg name="host" value="$(env CARLA_SIM_HOST)" />
  </include>
  <!-- spawn example ego vehicle -->
  <include file="$(find carla_ego_vehicle)/launch/carla_example_ego_vehicle.launch">
    <arg name="host" value="$(env CARLA_SIM_HOST)" />
  </include>
  <!-- spawn custom ego vehicle -->
  <!-- <include file="$(find carla_ego_vehicle)/launch/carla_ego_vehicle.launch">
    <arg name="host" value="$(env CARLA_SIM_HOST)" />
    <arg name="sensor_definition_file" value="$(env EGO_VEHICLE_SENSORS)" />
  </include> -->
  <!-- spectate the ego vehicle -->
  <!-- <include file="$(find carla_spectator_camera)/launch/carla_spectator_camera.launch">
    <arg name="host" value="$(env CARLA_SIM_HOST)" />
  </include> -->
  <!-- attach the Ackermann remote control to the ego vehicle -->
  <include file="$(find carla_ackermann_control)/launch/carla_ackermann_control.launch" />
</launch>
